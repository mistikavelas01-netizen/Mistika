# ===========================================
# Autenticación
# ===========================================
JWT_SECRET=
JWT_EXPIRES_IN=8h

# ===========================================
# URLs de la aplicación
# ===========================================
# Dominio público. Se usa en correos (enlaces a pedidos) y en la app.
# Desarrollo: http://localhost:3000
# Producción: https://tudominio.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# Email (Resend)
# ===========================================
RESEND_KEY=
RESEND_FROM_EMAIL=
RESEND_FROM_NAME=Mistika

# ===========================================
# Cloudinary (subida de imágenes)
# ===========================================
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=


# ===========================================
# Mercado Pago (Checkout Pro)
# ===========================================
# Access Token: panel MP → Tu app → Credenciales de prueba (pruebas) o Producción.
# El token puede empezar con APP_USR- en ambos casos.
MERCADOPAGO_ACCESS_TOKEN=

# Opcional: forzar checkout sandbox (para tarjetas de prueba) aunque no sea localhost (ej. ngrok).
# NEXT_PUBLIC_MERCADOPAGO_USE_SANDBOX=true

# Opcional: para validar firma x-signature de webhooks (panel MP → Webhooks).
# Si no lo configuras, el webhook sigue funcionando; con él se valida la petición.
# MERCADOPAGO_WEBHOOK_SECRET=

# ===========================================
# Firebase
# ===========================================
# La configuración de Firebase está en src/firebase/firebase.ts
# Para producción, usa variables de entorno y actualiza firebase.ts
# (ej: NEXT_PUBLIC_FIREBASE_* o FIREBASE_*)
